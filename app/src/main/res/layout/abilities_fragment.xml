<?xml version="1.0" encoding="utf-8"?>
<layout>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/list"
            layout="@layout/base_recycler"
            app:downloadInProgress="@{ViewModel.downloadInProgress}"
            app:isEmpty="@{ViewModel.isEmpty()}"
            app:refreshListener="@{refreshListener}" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            android:onClick="@{() -> UserActionListener.onAddAbilityClick()}"
            android:src="@drawable/plus"
            android:visibility="@{ViewModel.isOwn() ? View.VISIBLE : View.GONE}"
            app:backgroundTint="@color/colorPrimary"
            app:borderWidth="0dp"
            app:elevation="6dp"
            app:fabSize="normal" />

    </FrameLayout>

    <data>

        <import type="android.view.View" />

        <variable
            name="ViewModel"
            type="blagodarie.rating.ui.abilities.AbilitiesViewModel" />

        <variable
            name="refreshListener"
            type="androidx.swiperefreshlayout.widget.SwipeRefreshLayout.OnRefreshListener" />

        <variable
            name="UserActionListener"
            type="blagodarie.rating.ui.abilities.AbilitiesFragment.UserActionListener" />

    </data>

</layout>