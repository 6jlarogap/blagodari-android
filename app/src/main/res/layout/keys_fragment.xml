<?xml version="1.0" encoding="utf-8"?>
<layout>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="@dimen/activity_horizontal_margin"
        android:paddingVertical="@dimen/activity_vertical_margin">

        <include
            android:id="@+id/list"
            layout="@layout/base_recycler"
            app:downloadInProgress="@{viewModel.downloadInProgress}"
            app:isEmpty="@{viewModel.isEmpty()}"
            app:refreshListener="@{refreshListener}" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_margin"
            android:onClick="@{() -> UserActionListener.onAddKeyClick()}"
            android:src="@drawable/plus"
            android:visibility="@{viewModel.isOwn() ? View.VISIBLE : View.GONE}"
            app:backgroundTint="@color/colorPrimary"
            app:borderWidth="0dp"
            app:elevation="6dp"
            app:fabSize="normal" />

    </FrameLayout>

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="blagodarie.rating.ui.keys.KeysViewModel" />

        <variable
            name="refreshListener"
            type="androidx.swiperefreshlayout.widget.SwipeRefreshLayout.OnRefreshListener" />

        <variable
            name="UserActionListener"
            type="blagodarie.rating.ui.keys.KeysFragment.UserActionListener" />

    </data>
</layout>